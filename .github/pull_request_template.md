## Summary

<!-- What changed and why. One or two sentences is fine. -->

## Checklist

- [ ] Branched from `dev` and targeting `dev` (only release syncs target `main`)
- [ ] Commit messages use a conventional prefix (`feat:`, `fix:`, `chore:`, ...)
- [ ] `npm run check` passes locally
- [ ] Tests added or updated for behaviour changes
- [ ] Config or import format changes are documented in the README
